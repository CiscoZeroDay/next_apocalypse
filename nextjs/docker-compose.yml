version: "3.9"
services:
  app:
    build: .
    ports: ["3000:3000"]
    environment:
      - FLAG=CTF{next_middleware_should_not_be_your_only_guard}
    # For mitigation example, you can front with nginx and strip the internal header.
    # See nginx service example below (disabled by default).

  # nginx:
  #   image: nginx:alpine
  #   depends_on: [app]
  #   ports: ["8080:80"]
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro

