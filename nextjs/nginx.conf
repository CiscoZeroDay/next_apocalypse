worker_processes auto;
events { worker_connections 1024; }
http {
  server {
    listen 80;
    location / {
      proxy_pass http://app:3000;
      # Mitigation: strip the internal header so bypass attempts fail
      proxy_set_header x-middleware-subrequest "";
    }
  }
}
